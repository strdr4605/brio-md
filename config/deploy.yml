# Base Kamal config - shared settings for all Brio.md apps
builder:
  arch: amd64

registry:
  username: strdr4605
  password:
    - KAMAL_REGISTRY_PASSWORD

aliases:
  apps: server exec docker exec kamal-proxy kamal-proxy list
